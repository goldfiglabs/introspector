version: 1
resources:
  - category: Role
    service: iam
    provider_type: InstanceProfile
    name: InstanceProfileName
    uri:
      uri: Arn
    attributes:
      provider: [Path, InstanceProfileName, InstanceProfileId, Arn, CreateDate]
    relations:
      - path: Roles
        relation: contains
        uri:
          uri:
            path: Arn
      - path: Roles
        relation: manages
        invert: true
        uri:
          uri:
            path: Arn
